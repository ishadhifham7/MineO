/**
 * Re-export useAuth hook from AuthProvider
 * This maintains consistency with the project's hooks folder structure
 */
export { useAuth } from "../providers/AuthProvider";
export type { AuthUser } from "../providers/AuthProvider";
